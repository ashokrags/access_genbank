```
(cbc_conda_v1) [aragaven@login003 access_genbank]$ python download_genbank_genomes.py -h
usage: download_genbank_genomes.py [-h] [-b BASE_FTP_PATH] [-ad ASSEMBLY_DIR]
                                   [-ft FILE_TYPE_TO_SEARCH]
                                   [-se SPECIES_TO_EXCLUDE] [-c CONCATENATE]
                                   [-o OUTPUT_DIR] [-ol LOG_DIR] [-dr DRY_RUN]

optional arguments:
  -h, --help            show this help message and exit
  -b BASE_FTP_PATH, --base_ftp_path BASE_FTP_PATH
                        provide the base path for the directories to be
                        queried
  -ad ASSEMBLY_DIR, --assembly_dir ASSEMBLY_DIR
                        provide the base path for the directories to be
                        queried
  -ft FILE_TYPE_TO_SEARCH, --file_type_to_search FILE_TYPE_TO_SEARCH
                        provide the base path for the directories to be
                        queried
  -se SPECIES_TO_EXCLUDE, --species_to_exclude SPECIES_TO_EXCLUDE
                        provide the base path for the directories to be
                        queried
  -c CONCATENATE, --concatenate CONCATENATE
                        provide the base path for the directories to be
                        queried
  -o OUTPUT_DIR, --output_dir OUTPUT_DIR
                        provide the base path for the directories to be
                        queried
  -ol LOG_DIR, --log_dir LOG_DIR
                        provide the base path for the directories to be
                        queried
  -dr DRY_RUN, --dry_run DRY_RUN
                        provide the base path for the directories to be
                        queried
```